<h1>
    <%= @product.title %>
</h1>
<p>
    <%= @product.description %>
</p>
<p>
    <%= number_to_currency_rub(@product.price) %>
</p>
<%= button_to 'Добавить в корзину', line_items_path(product_id: @product.id), class: 'btn btn-primary' %>
