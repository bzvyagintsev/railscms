<%= form_for [:admin, @pages_category], html: {class: "form-horizontal"} do |f| %>
    <% if @pages_category.errors.any? %>
        <div class="alert alert-danger" role="alert">
            <h4><%= pluralize(@pages_category.errors.count, "error") %> prohibited this page from being saved:</h4>
            <ul>
                <% @pages_category.errors.full_messages.each do |message| %>
                    <li>
                        <%= message %>
                    </li>
                    <% end %>
            </ul>
        </div>
        <% end %>
            <div class="form-group">
                <label for="" class="col-md-2 control-label">Название</label>
                <div class="col-md-10">
                    <%= f.text_field :title, class: 'form-control' %>
                </div>
            </div>
            <div class="form-group">
                <div class="col-md-12">
                    <%= button_tag(type: 'submit', class: 'btn btn-primary pull-right') do %>
                        <i class=""></i> Отправить
                        <% end %>
                </div>
            </div>
            <% end %>
